# App A — Sentiva RAG agent API. FastAPI/uvicorn are pre-installed on Databricks Apps
# but we pin the extras the agent needs.
fastapi
uvicorn[standard]
psycopg[binary]
databricks-sdk>=0.81.0
mlflow>=3.14.0
requests
pydantic>=2.0
# Tool-calling agent via the Databricks-recommended ChatDatabricks client (see gateway_chat.py).
databricks-langchain==0.20.0
langgraph==1.2.11
langgraph-prebuilt==1.1.0
mcp==1.29.1
